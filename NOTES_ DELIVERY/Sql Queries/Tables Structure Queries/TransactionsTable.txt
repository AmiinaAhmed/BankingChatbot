CREATE TABLE TRANSACTION
(	TRANSACTIONNO NUMBER(3,0) PRIMARY KEY, 
	TRANSACTIONTYPE VARCHAR2(15 BYTE) NOT NULL,
	TRANSACTIONAMOUNT NUMBER(7,0) NOT NULL,
	ACCOUNTID NUMBER(2,0) NOT NULL 
	REFERENCES ACCOUNT (ACCOUNTID), 
	TRANSACTIONDATE DATE NOT NULL 
) ;