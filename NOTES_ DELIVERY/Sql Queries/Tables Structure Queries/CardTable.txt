CREATE TABLE CARD
(	CARDNO NUMBER(14,0) PRIMARY KEY, 
	CARDBALANCELIMIT NUMBER(10,0) NOT NULL, 
	CARDENDDATE DATE NOT NULL,
	CARDAMOUNT NUMBER(8,0) NOT NULL, 
	ACCOUNTID NUMBER(2,0) NOT NULL 
	REFERENCES ACCOUNT (ACCOUNTID)
) ;
 
